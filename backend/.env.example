# Server-side only. Enable Directions API + Geocoding API + Places API (autocomplete) in Google Cloud.
GOOGLE_MAPS_API_KEY=

# Optional: restrict CORS to your Next.js origin in production.
CORS_ORIGINS=http://localhost:3000
